{
  "name": "combine-tools",
  "version": "0.1.0",
  "description": "A tool to comnbine IronFish notes",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "build": "rm -rf dist && tsc"
  },
  "private": true,
  "author": "iflabsfish",
  "dependencies": {
    "@ironfish/sdk": "^2.12.0"
  },
  "devDependencies": {
    "typescript": "5.0.4"
  }
}
